# 移动端开发快速开始

## 🚀 5分钟上手

### Step 1: 安装 Ant Design Mobile

```bash
cd frontend
npm install antd-mobile
```

### Step 2: 创建目录结构

```bash
# 创建移动端目录
mkdir -p src/pages/mobile
mkdir -p src/components/mobile
mkdir -p src/layouts

# 移动现有桌面版页面（可选，暂时不动）
# mv src/pages/*.tsx src/pages/desktop/
```

### Step 3: 创建 MobileLayout

```typescript
// src/layouts/MobileLayout.tsx
import { Outlet } from 'react-router-dom'
import { TabBar } from 'antd-mobile'
import { AppOutline, UnorderedListOutline } from 'antd-mobile-icons'

export default function MobileLayout() {
  return (
    <div style={{ height: '100vh', display: 'flex', flexDirection: 'column' }}>
      {/* 主内容 */}
      <div style={{ flex: 1, overflow: 'auto', paddingBottom: 50 }}>
        <Outlet />
      </div>

      {/* 底部导航 */}
      <div style={{ position: 'fixed', bottom: 0, left: 0, right: 0 }}>
        <TabBar>
          <TabBar.Item key="home" icon={<AppOutline />} title="首页" />
          <TabBar.Item key="plants" icon={<UnorderedListOutline />} title="植物" />
        </TabBar>
      </div>
    </div>
  )
}
```

### Step 4: 创建第一个移动端页面

```typescript
// src/pages/mobile/MobileHome.tsx
import { Typography } from 'antd-mobile'

export default function MobileHome() {
  return (
    <div style={{ padding: 16 }}>
      <Typography.Title>🌱 植物管家</Typography.Title>
      <p>欢迎使用移动端！</p>
    </div>
  )
}
```

### Step 5: 更新 App.tsx 添加移动端路由

```typescript
// src/App.tsx
import { BrowserRouter, Routes, Route } from 'react-router-dom'
import DesktopLayout from './layouts/DesktopLayout'  // 现有
import MobileLayout from './layouts/MobileLayout'   // 新增

// 导入页面
const DesktopDashboard = lazy(() => import('./pages/Dashboard'))
const MobileHome = lazy(() => import('./pages/mobile/MobileHome'))

function App() {
  return (
    <BrowserRouter>
      <Routes>
        {/* 桌面版（现有） */}
        <Route path="/" element={<DesktopLayout />}>
          <Route path="dashboard" element={<DesktopDashboard />} />
          {/* 其他路由... */}
        </Route>

        {/* 移动端（新增） */}
        <Route path="/mobile" element={<MobileLayout />}>
          <Route index element={<MobileHome />} />
        </Route>
      </Routes>
    </BrowserRouter>
  )
}
```

### Step 6: 测试

```bash
# 启动开发服务器
npm run dev

# 访问
# 桌面版: http://localhost:12800/
# 移动端: http://localhost:12800/mobile
```

---

## 📱 下一步

1. ✅ 完成基础配置
2. 📖 阅读完整文档: `docs/15-移动端独立版本实施指南.md`
3. 🎨 开始开发移动端页面
4. 🧪 真机测试

---

**需要帮助？**

查看详细文档：`docs/15-移动端独立版本实施指南.md`
