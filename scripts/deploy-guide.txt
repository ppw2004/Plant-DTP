╔══════════════════════════════════════════════════════════════╗
║         植物数字孪生平台 - 生产环境部署指南                      ║
╚══════════════════════════════════════════════════════════════╝

📋 准备工作已完成：
  ✅ 前端已构建（frontend/dist）
  ✅ Nginx 配置文件已创建
  ✅ systemd 服务文件已创建
  ✅ 部署脚本已准备

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🚀 部署步骤（在终端中执行）：

第1步：进入项目目录
    cd /home/pengpeiwen/Plant-DTP

第2步：执行部署脚本
    sudo bash scripts/setup-production.sh

第3步：验证部署（在浏览器中打开）
    http://82.156.213.38

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📊 部署前后对比：

  开发环境                →  生产环境
  ──────────────────────────────────────
  :12800 (前端端口)        →  :80 (默认端口)
  :12801 (后端端口)        →  通过 Nginx 代理
  手动启动服务             →  开机自动启动
  崩溃需手动重启           →  自动重启
  需要记住端口号           →  直接访问 IP

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔧 部署后管理命令：

  查看后端日志：
    tail -f /tmp/plant-backend.log

  重启后端服务：
    sudo systemctl restart plant-dtp-backend

  重启 Nginx：
    sudo systemctl restart nginx

  查看服务状态：
    sudo systemctl status plant-dtp-backend
    sudo systemctl status nginx

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✨ 部署完成后的效果：

  访问地址：http://82.156.213.38
  API 地址：http://82.156.213.38/api/v1/plants

  • 无需输入端口号
  • 前端加载速度更快
  • 服务自动重启
  • 更稳定可靠

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

如有问题，检查日志：
  • Nginx 错误日志: sudo tail -f /var/log/nginx/error.log
  • 后端日志: tail -f /tmp/plant-backend.log

═══════════════════════════════════════════════════════════════
